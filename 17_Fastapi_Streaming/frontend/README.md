# React Dashboard

This folder contains the pre-built React dashboard for the Streaming Productivity Assistant practical.

## Run

```bash
cd frontend
npm install
npm run dev
```

Open the local URL printed by Vite, normally `http://127.0.0.1:5173`.

The backend must already be running at `http://127.0.0.1:8000`.
